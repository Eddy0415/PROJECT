import { Routes } from '@angular/router'; // routes type
import { SearchComponent } from './search'; // standalone page

export const SEARCH_ROUTES: Routes = [
  { path: '', component: SearchComponent }, // /search
];
